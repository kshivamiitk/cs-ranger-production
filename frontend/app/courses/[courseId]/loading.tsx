import { AppRouteLoading } from '@/components/loading/AppRouteLoading';

export default function Loading() {
  return <AppRouteLoading label="Loading course" description="Fetching modules, progress, and access state." />;
}
